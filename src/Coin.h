//initialize pin c4 that will be a switch that will  simulate  a coin going into the machine
void CoinInit(void);
//check if the player has paid (clicked  the  switch)
//returns 0 if no coin 1  if coin
int Paid(void);
